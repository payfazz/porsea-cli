Create your styling inside this folder, if your project running two or more framework (scss and stylus), you can separate into two folder
inside this folder.

-- styles
   -- scss
      myscss.scss
   -- stylus
      mystylus.styl